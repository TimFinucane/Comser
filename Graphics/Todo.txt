Implement the FreeType libs

Get OpenGL working

Spritesheet working

Control window from here (what did he mean by this?)

Research if its possible to have stronger types
 and static info for mesh vertices so we can do
 BufferInfo effectively statically.

Exception safety for shaders, throwing and such too

More functionality for changing ObjectArrays

Changing some exceptions to fail states for potential failures in shaders
 (e.g. compilation failures should not throw exceptions, as they are too common. Zombie state instead?)